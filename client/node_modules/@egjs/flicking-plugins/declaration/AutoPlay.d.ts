import Flicking, { Plugin, Direction } from "@egjs/flicking";
interface AutoPlayOptions {
    duration: number;
    direction: Direction[keyof Direction];
    stopOnHover: boolean;
}
declare class AutoPlay implements Plugin {
    private duration;
    private direction;
    private stopOnHover;
    private flicking;
    private timerId;
    private mouseEntered;
    constructor(options?: Partial<AutoPlayOptions>, direction?: AutoPlayOptions["direction"]);
    init(flicking: Flicking): void;
    destroy(): void;
    play: () => void;
    stop: () => void;
    private onMouseEnter;
    private onMouseLeave;
}
export default AutoPlay;
